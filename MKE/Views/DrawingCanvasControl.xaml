<UserControl x:Class="MKE.DrawingCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:vm="clr-namespace:MKE.ViewModels"
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:MKE.Services"
             DataContext="{Binding Source={x:Static local:ViewModelLocator.DrawingCanvasViewModel}}"
             d:DesignHeight="800" d:DesignWidth="1200"
             Height="800" Width="1200">
    <!--Loaded="OnControlLoaded">-->
    
    <!-- Set the Height and Width properties directly -->
    <Canvas Name="DrawingCanvas" Background="LightBlue" VerticalAlignment="Stretch" >
        <!-- Changed VerticalAlignment to Stretch -->
        <Canvas.Resources>
            <Style x:Key="GridLineStyle" TargetType="{x:Type Line}">
                <Setter Property="Stroke" Value="Gray"/>
                <Setter Property="StrokeThickness" Value="1"/>
            </Style>
        </Canvas.Resources>

        <ItemsControl ItemsSource="{Binding VerticalLines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}" 
                  Style="{StaticResource GridLineStyle}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ItemsControl ItemsSource="{Binding HorizontalLines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}" 
                  Style="{StaticResource GridLineStyle}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        

    </Canvas>
</UserControl>
